"use strict";

function createKlaviyoOAuthHandlers({config,requireConnectAccess,createTransaction,consumeTransaction,sendAuthorizationResponse,createPkce,exchangeToken,saveConnection,getConnection,resolveAccount,parseExpiry}={}){
  if(!config||typeof config!=="object")throw new TypeError("config is required");
  for(const[name,fn]of Object.entries({requireConnectAccess,createTransaction,consumeTransaction,sendAuthorizationResponse,createPkce,exchangeToken,saveConnection,getConnection,resolveAccount,parseExpiry}))if(typeof fn!=="function")throw new TypeError(`${name} must be a function`);
  async function start(req,res){try{const access=await requireConnectAccess(req,res);if(!access)return;if(!config.clientId||!config.clientSecret||!config.redirectUri)throw new Error("Missing Klaviyo env");const{verifier,challenge}=createPkce(),{state}=await createTransaction(access.userId,"klaviyo",config.redirectUri,verifier);const params=new URLSearchParams({response_type:"code",client_id:config.clientId,redirect_uri:config.redirectUri,scope:config.scopes,state,code_challenge_method:"S256",code_challenge:challenge});return sendAuthorizationResponse(req,res,`${config.authorizationBase}/oauth/authorize?${params}`)}catch(error){return res.status(500).send(error.message)}}
  async function callback(req,res){try{const{code,state,error,error_description:description}=req.query;if(error)return res.redirect(`/dashboard?klaviyo_error=${encodeURIComponent(description||error)}`);if(!code)return res.redirect("/dashboard?klaviyo_error=missing_code");const transaction=await consumeTransaction(state,"klaviyo",config.redirectUri);if(!transaction)return res.redirect("/dashboard?klaviyo_error=invalid_state");if(!transaction.pkce_verifier)return res.redirect("/dashboard?klaviyo_error=missing_code_verifier");const data=await exchangeToken({code,verifier:transaction.pkce_verifier,redirectUri:config.redirectUri});const userId=transaction.user_id;await saveConnection(userId,"klaviyo",{accessToken:data.access_token,refreshToken:data.refresh_token||null,tokenExpiresAt:parseExpiry(data.expires_in),accountId:null,accountName:null,metadata:{scope:data.scope||config.scopes,tokenType:data.token_type||null,expiresIn:data.expires_in||null,selectedPlatformAccountId:null,selectedPlatformAccountIds:[],selectedPlatformAccounts:[],lastOwnedPlatformAccountId:null,accountSelectionRequired:true,reconnectSelectionRequired:true,accountSelectionGuardVersion:"v2-explicit-selection"}});const account=await resolveAccount(await getConnection(userId,"klaviyo"));await saveConnection(userId,"klaviyo",{accountId:null,accountName:null,metadata:{selectedPlatformAccountId:null,selectedPlatformAccountIds:[],selectedPlatformAccounts:[],lastDiscoveredPlatformAccountId:account.platform_account_id,availableAccounts:[{platform_account_id:account.platform_account_id,account_name:account.account_name,currency:account.currency||null}],accountSelectionRequired:true,accountSelectionGuardVersion:"v2-explicit-selection",rawAccount:account.raw_account}});return res.redirect("/dashboard?klaviyo_connected=1&account_selection_required=1")}catch(error){return res.redirect(`/dashboard?klaviyo_error=${encodeURIComponent(error.message)}`)}}
  return Object.freeze({start,callback});
}
module.exports={createKlaviyoOAuthHandlers};
