"use strict";
function createGoogleSheetsOAuthHandlers({scopes,defaultWorksheet,requireConnectAccess,createTransaction,consumeTransaction,sendAuthorizationResponse,getRedirectUri,createClient,getConnection,saveConnection,now=()=>new Date().toISOString()}={}){
 for(const[name,fn]of Object.entries({requireConnectAccess,createTransaction,consumeTransaction,sendAuthorizationResponse,getRedirectUri,createClient,getConnection,saveConnection,now}))if(typeof fn!=="function")throw new TypeError(`${name} must be a function`);
 async function start(req,res){try{const access=await requireConnectAccess(req,res);if(!access)return;const redirectUri=getRedirectUri(req),{state}=await createTransaction(access.userId,"google_sheets",redirectUri);const url=createClient(req).generateAuthUrl({access_type:"offline",prompt:"consent",include_granted_scopes:true,state,scope:scopes});return sendAuthorizationResponse(req,res,url)}catch(error){return res.status(500).send(error.message)}}
 async function callback(req,res){try{const{code,state,error,error_description:description}=req.query;if(error)return res.redirect(`/dashboard?google_sheets_error=${encodeURIComponent(description||error)}`);if(!code)return res.redirect("/dashboard?google_sheets_error=missing_code");const redirectUri=getRedirectUri(req),transaction=await consumeTransaction(state,"google_sheets",redirectUri);if(!transaction)return res.redirect("/dashboard?google_sheets_error=invalid_state");const userId=transaction.user_id,{tokens}=await createClient(req).getToken(code);if(!tokens.access_token&&!tokens.refresh_token)throw new Error("Google Sheets token exchange returned no token");const existing=await getConnection(userId,"google_sheets");await saveConnection(userId,"google_sheets",{accessToken:tokens.access_token||existing?.access_token||null,refreshToken:tokens.refresh_token||existing?.refresh_token||null,tokenExpiresAt:tokens.expiry_date?new Date(tokens.expiry_date).toISOString():(existing?.token_expires_at||null),accountId:null,accountName:null,metadata:{googleSheetsOAuthVersion:"v1",scope:tokens.scope||scopes.join(" "),tokenType:tokens.token_type||null,connectedAt:now(),selectedPlatformAccountId:null,selectedPlatformAccountIds:[],selectedPlatformAccounts:[],lastOwnedPlatformAccountId:null,accountSelectionRequired:true,reconnectSelectionRequired:true,spreadsheet_id:null,spreadsheet_name:null,worksheet_name:defaultWorksheet,auto_sync_enabled:false,last_sync_at:null,last_sync_status:null,last_sync_error:null}});return res.redirect("/dashboard?google_sheets_connected=1&account_selection_required=1")}catch(error){return res.redirect(`/dashboard?google_sheets_error=${encodeURIComponent(error.message)}`)}}
 return Object.freeze({start,callback});
}module.exports={createGoogleSheetsOAuthHandlers};
